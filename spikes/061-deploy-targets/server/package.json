{
  "name": "klaridian-deploy-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "dist/server.bundle.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && npm run bundle",
    "bundle": "esbuild dist/index.js --bundle --platform=node --target=node20 --format=esm --outfile=dist/server.bundle.js",
    "start": "node dist/server.bundle.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.2.0",
    "@modelcontextprotocol/node": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3",
    "esbuild": "^0.24.0"
  },
  "license": "MIT"
}
