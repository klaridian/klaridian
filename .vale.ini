Vocab = klaridian
StylesPath = .github/vale/styles
MinAlertLevel = warning

Packages = Microsoft

[*.md]
BasedOnStyles = Vale, Microsoft

# "agent" is AI/MCP domain terminology (an LLM-driven client calling tools),
# not a Microsoft-support-agent reference — disable the suggested swap to
# "personal digital assistant", which would be actively wrong here.
Microsoft.Terms = NO

[*.mdx]
BasedOnStyles = Vale, Microsoft
Microsoft.Terms = NO
