[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "mcpforge-posthog-middleware"
version = "0.1.0"
description = "FastMCP middleware: PostHog product-observability event per tool call. The Python counterpart to mcpforge's TypeScript posthog plugin."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [{ name = "Ricardo Vasconcelos" }]
dependencies = [
    "fastmcp>=3.0.0",
    "posthog>=3.0.0",
]

[project.urls]
Homepage = "https://github.com/ricardocvasconcelos/mcpforge"
Repository = "https://github.com/ricardocvasconcelos/mcpforge"

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.24.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/mcpforge_posthog_middleware"]

[tool.pytest.ini_options]
asyncio_mode = "auto"

